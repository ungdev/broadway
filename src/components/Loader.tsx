import React from 'react';

import './Loader.scss';

const Loader = () => (
	<div className="loader">
		<i className="fas fa-spinner fa-spin loader-icon" /> Chargement...
	</div>
);

export default Loader;
